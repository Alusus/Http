/*
 * مثال لإنشاء خادم HTTP باستخدام لغة الأسس ومكتبة Http.
 *
 * حقوق النشر © 2026 سرمد عبد الله
 *
 * هذه المكتبة برمجية حرة؛ يمكنك إعادة توزيعها و/أو تعديلها بموجب شروط
 * رخصة غنو العمومية الصغرى (LGPL) كما نشرتها مؤسسة البرمجيات الحرة؛ إما الإصدار
 * 3 من الرخصة، أو (حسب اختيارك) أي إصدار لاحق.
 *
 * توزع هذه المكتبة على أمل أن تكون مفيدة، ولكن دون أي ضمان؛ دون حتى
 * الضمان الضمني لقابلية التسويق أو الملاءمة لغرض معين. راجع رخصة غنو
 * العمومية الصغرى لمزيد من التفاصيل.
 *
 * من المفترض أن تكون قد تلقيت نسخة من رخصة غنو العمومية الصغرى مع
 * هذه المكتبة؛ وإلا فاحصل عليها من <https://www.gnu.org/licenses/>.
 */

اشمل "مـتم/طـرفية.أسس"؛
اشمل "مـتم/نـص.أسس"؛
اشمل "مـتم/ذاكـرة.أسس"؛
اشمل "مـحا"؛
مـحا.اشمل_ملف("Alusus/Http"، "بـننت.أسس")؛

وحدة أخـتبار_الوحدة {
    استخدم مـتم؛

    دالة ابدا() {
        عرف سياق: مؤشر[بـننت.سـياق] = بـننت.شغل_الخادم(طلب_المنادى~مؤشر, "هذه بيانات مخصصة"، "8080")؛
        طـرفية.اطبع("شُغّل الخادم على المنفذ 8080.\جhttp://localhost:8080\جاضغط زر الإدخال لإغلاق الخادم.")؛
        طـرفية.أدخل_محرفا()؛
        بـننت.أوقف_الخادم(سياق)؛
        طـرفية.اطبع("أغلق الخادم.\جاضغط زر الإدخال للخروج.")؛
        طـرفية.أدخل_محرفا()؛
    }


    دالة طلب_المنادى (اتصال: مؤشر[بـننت.اتـصال]): صحيح{
        عرف معلومات_الطلب: مؤشر[بـننت.مـعلومات_الطلب] = بـننت.هات_معلومات_الطلب(اتصال)؛ 
        عرف سياق: مصفوفة[محرف, 1024]؛

        نـص.عيّن(
            سياق~مؤشر, "<div dir=rtl><h1>مرحبًا بك في خادم ب.ن.ن.ف للغة الأسس</h1><p>أنت الآن تتصفح \"%s\"</p><p>%s</p></div>",
            معلومات_الطلب~محتوى.المعرف_المحلي،
            معلومات_الطلب~محتوى.بيانات_المستخدم
        )؛
        بـننت.اطبع(اتصال, "HTTP/1.1 200 OK\r\n")؛
        بـننت.اطبع(اتصال,  "Content-Type: text/html; charset=utf-8\r\n")؛
        بـننت.اطبع(اتصال,  "Content-Length: %d\r\n\r\n", نـص.هات_الطول(سياق~مؤشر))؛
        بـننت.اطبع(اتصال, سياق~مؤشر)؛

        أرجع 1؛
    }
}

أخـتبار_الوحدة.ابدا()؛

